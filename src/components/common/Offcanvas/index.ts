import { Component } from '@/libs/@types';

import Base, { BaseProps } from '@/components/common/Offcanvas/Base';

export type * from '@/components/common/Offcanvas/Base';

export default Object.assign<Component<BaseProps>, {}>(Base, {});
