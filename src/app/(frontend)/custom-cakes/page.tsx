import React from 'react';

import { PageProps } from '@/libs/@types';

import CustomCakesIndex from '@/components/pages/CustomCakesIndex';

const Page = ({}: PageProps): React.ReactElement => {
    return <CustomCakesIndex />;
};

export default Page;
