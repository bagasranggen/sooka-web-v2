import React from 'react';

import OrderConfirmationIndex from '@/components/pages/OrderConfirmationIndex';

export type PageProps = {};

const Page = ({}: PageProps): React.ReactElement => {
    return <OrderConfirmationIndex />;
};

export default Page;
